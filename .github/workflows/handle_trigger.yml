name: Handle Trigger from Developing Repository

on:
  repository_dispatch:
    types:
      - triggered_by_developing  # Matches the event_type sent from Developing

jobs:
  run_tests:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the Testing repository
      - name: Checkout Testing Repository
        uses: actions/checkout@v4

      # Step 2: Perform some action (e.g., run tests or log the event)
      - name: Log Event
        run: echo "Triggered by Developing repository"

      # Additional steps can be added here as needed
